<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search</title>
</head>
<body>
    <% let flag=0; %>
        <% for(let i=0;i<studentsArray.length;i++){
            if(studentsArray[i].roll === yourRoll){ %>
                <p>
                    <% flag=1; %>
                    <% const j=JSON.stringify(studentsArray[i]) %>
                    <%= j %>
                </p>
           <% }
        } %>
        <% if(flag==0){ %>
            <p>
                Roll does not exist!
            </p>
        <% } %>
</body>
</html>