<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>
<body>
    <% let flag=0; %>
    <% for(let i=0;i<studentsArray.length;i++){
        if(studentsArray[i].roll === yourRoll){ %>
            <p>
                <% flag=1; %>
                <form action="/editStudent" method="post">
                    <p>Roll :<%= yourRoll %></p>
                    <label for="eroll">Your Roll : </label>
                    <input type="text" name="eroll" value="<%= yourRoll %>" readonly>
                    <label for="name">Enter Name:</label>
                    <input type="text" name="nameEdit" placeholder="Your Name">
                    <input type="submit" value="Edit Data">
                </form>
                <input type="submit" onclick="cancelForm()" value="Cancel">
            </p>
       <% }
    } %>
    <% if(flag==0){ %>
        <p>
            Roll does not exist!
        </p>
    <% } %>
    <script>
        function cancelForm(){
            window.location.href="http://localhost:3000";
        }
    </script>
</body>
</html>